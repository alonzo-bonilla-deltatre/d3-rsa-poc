export type ImageTransformations = {
  mobile: string;
  tablet: string;
  desktop: string;
  mobileWidth: number;
  mobileHeight: number;
};

export type ImageAsset = {
  title: string;
  templateUrl: string;
  format: string;
  slug: string;
};
