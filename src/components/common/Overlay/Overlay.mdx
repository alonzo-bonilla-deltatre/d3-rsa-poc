import * as OverlayStories from './Overlay.stories';

<Meta of={OverlayStories} />;

# Overlay

Use the _Overlay_ component to show an overlay component with some content passed as children prop. Remember to pass an
_onCloseHandler_ function

### Default

The default version with black background and centered children content

<Canvas of={OverlayStories.Default} />

<Controls />
